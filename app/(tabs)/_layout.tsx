import React from 'react';
import { Slot } from 'expo-router';

// simple layout with no tabs; all navigation will be handled by modals in the screen
export default function Layout() {
  return <Slot />;
}
